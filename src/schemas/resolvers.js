const { AuthenticationError } = require('apollo-server-express');
const { User } = require('../../server/models');

const resolvers = {
    Query: {
         
    },
    Mutations: {
        
    }
}
